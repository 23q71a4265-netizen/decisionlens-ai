# Optional. If set, natural-language queries are answered by Claude
# (given the already-computed stats, not raw data) instead of the
# built-in rule-based engine. The app works fully without this.
ANTHROPIC_API_KEY=
